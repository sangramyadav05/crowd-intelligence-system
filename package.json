{
  "name": "crowd-intelligence-system",
  "private": true,
  "version": "1.0.0",
  "description": "Unified full-stack foundation for the Crowd Intelligence System hackathon project.",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix backend run dev",
    "client": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
